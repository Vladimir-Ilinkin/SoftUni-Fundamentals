function aSCIIValuesReversedCharacters(param1, param2, param3) {
    let lineOne = `` + param3 + param2 + param1;
    console.log(lineOne);
    console.log(
      `${param3.charCodeAt()} ${param2.charCodeAt()} ${param1.charCodeAt()}`
    );
  }